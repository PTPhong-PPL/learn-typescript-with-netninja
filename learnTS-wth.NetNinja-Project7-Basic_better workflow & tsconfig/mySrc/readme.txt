
// bật terminal -> chạy 'tsc --init' để tự động tạo file tsconfig.json nhằm điều chỉnh đầu ra của js

// khi đã setting xong config có thể chay 'tsc -w' mà không cần chỉ rõ, vì nó sẽ tự động compile tất cả .js trong src và output ra thư mục
trong phần output của config